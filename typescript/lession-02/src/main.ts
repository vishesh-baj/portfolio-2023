let myName: string = "Vishesh";
let meaningOfLife: number = 0;
let isLoading: boolean;
let album: any;
isLoading = true;
album = 5150;
const sum = (a: number, b: string) => {
  return a + b;
};

let postId: string | number;
let isActive: number | boolean | string;

let re: RegExp = /\w+/g;
